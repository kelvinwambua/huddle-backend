version: "2"
sql:
    - engine: "postgresql"
      queries: "sql/queries"
      schema: "migrations"
      gen:
          go:
              package: "sqlc"
              out: "internal/database/sqlc"
              emit_json_tags: true
              emit_interface: true
              emit_empty_slices: true
